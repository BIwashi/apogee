"""apogee hooks — wire-compatible Python hook library for Claude Code.

This package is deliberately dependency-free so that it can be dropped into a
project's ``.claude/hooks`` directory and invoked from Claude Code's hook
configuration without requiring ``uv``, ``pip``, or a virtualenv.
"""
